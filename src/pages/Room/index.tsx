import React from 'react';
import FixedView from '../_shared/Layouts/FixedView';

const Room = () => {
  React.useEffect(() => {}, []);

  return <FixedView>ROOM!</FixedView>;
};

export default Room;
